# Zinat Al-Haya School Management - Production Environment Configuration
# IMPORTANT: Use your hosting platform's environment variable settings
# This file is for reference only - DO NOT commit to version control

# Application Environment
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Database Configuration
# Replace with your actual Railway PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@host:port/database

# Alternative Database Configuration (if not using DATABASE_URL)
DATABASE_TYPE=postgres
DATABASE_HOST=your-railway-db-host
DATABASE_PORT=5432
DATABASE_USERNAME=your-db-username
DATABASE_PASSWORD=your-db-password
DATABASE_NAME=your-db-name

# JWT Configuration
# IMPORTANT: Generate strong, unique secret keys for production
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-make-it-very-long-and-random-at-least-64-characters
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here-make-it-different-from-jwt-secret
JWT_REFRESH_EXPIRES_IN=7d

# File Upload Configuration
UPLOAD_DEST=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx

# CORS Configuration
# Replace with your actual Vercel frontend URL
CORS_ORIGIN=https://your-frontend-app.vercel.app
CORS_CREDENTIALS=true

# Additional CORS origins (comma-separated for multiple domains)
# CORS_ADDITIONAL_ORIGINS=https://your-custom-domain.com,https://admin.yourschool.com

# Logging Configuration
LOG_LEVEL=info

# Security Configuration
BCRYPT_SALT_ROUNDS=12

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Application Info
APP_NAME=Zinat Al-Haya School Management
APP_VERSION=1.0.0
APP_DESCRIPTION=Complete bilingual school management system for kindergarten

# Session Configuration
SESSION_SECRET=another-super-secret-key-for-sessions-make-it-different-from-jwt

# Database Pool Configuration
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_IDLE_TIMEOUT=30000
DB_POOL_ACQUIRE_TIMEOUT=60000

# API Configuration
API_PREFIX=/api
API_VERSION=v1

# Email Configuration (optional - for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@zinatalhaya.com

# Backup Configuration (optional)
# BACKUP_ENABLED=true
# BACKUP_SCHEDULE=0 2 * * *
# BACKUP_RETENTION_DAYS=30

# Monitoring and Analytics (optional)
# SENTRY_DSN=https://your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-new-relic-key
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Feature Flags
ENABLE_SWAGGER=false
ENABLE_METRICS=true
ENABLE_HEALTH_CHECK=true
ENABLE_CORS_LOGGING=false

# SSL Configuration (if needed)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Timezone
TZ=Asia/Muscat
